\n## Why this is happening (diagnosis from your Actor Health screen)\n\nYour screen is telling a clear story:\n\n| Actor | Health | Real story |\n|---|---|---|\n| `misceres~indeed-scraper` (Indeed) | **15% — Failing** | The actor itself is fine — it produced **82 raw items** (most of any source). Failures are **timeouts** because we ask it to crawl too much in 60s, plus **3 invalid-input errors** for unsupported countries (Serbia/Croatia/etc. — Indeed only supports the ISO list we hardcoded). |\n| `apify~web-scraper` (classifieds + career_page) | 58–67% — Degraded | Generic Puppeteer scraper crawling 10–12 Google result pages. 100–200s avg = right on the timeout edge. |\n| `apify~facebook-posts-scraper` | 83% — Healthy | Works, but **only 4 of 50 items kept** — Facebook posts rarely contain hiring-intent keywords, so prefilter drops them. |\n| `vdrmota~contact-info-scraper` (Hunter replacement) | 100% | Works fine — only 1 run because it only fires when leads need email enrichment. |\n\n**Root causes, ranked by impact:**\n\n1. Indeed is by far the **best lead source** (82 items, real employers, real contact info), but it's the **least country-flexible** and timing out from oversized queries.\n2. Country list is too narrow (10 countries). User asks for **full Balkan + EU**.\n3. We treat all sources equally — Facebook gets the same wave slot as Indeed even though Indeed produces 5x the keepable leads.\n4. No \ ADD GOOGLE SEARCH GEMINI GOOGFLE CSE WHATVER U CXAN ADD ADDD AND MAKE IT A ROBUST DETAILED DEEP ANAylitic great multi million dollar grade lead and signaling demand aquiring system 